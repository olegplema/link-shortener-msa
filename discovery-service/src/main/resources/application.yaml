spring:
  application:
    name: discovery-service
server:
  port: 8761 # TODO change it to env variable
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
logging:
  level:
    com.netflix.eureka: OFF
    com.netflix.discovery: OFF